# accessories/

Parking lot for things that aren't part of the active EDF surface but are kept for reference.

- `legacy-docs/` — earlier-snapshot docs (banners explain status; see `../docs/historical-context.md` for index).
- (future) `plumb/` — agency design system (currently in `AppDev/`; will land here once `AppDev/` sort completes).
- (future) `research-vault/` — non-EDF research artifacts (currently in `AppDev/`).

Nothing in here is load-bearing for current work. If something is needed, it should be promoted out into the active surface (`docs/`, `libraries/`, `artifacts/`) deliberately.
